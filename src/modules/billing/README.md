# billing module (scaffold only)

Orders (POS-style creation) + KDS kanban board. See
`POSCountr-UI-Planning/poscountr-ui-page-inventory.md` section D5.

Scheduled for **F6 — Billing (Orders + KDS)**. Route wired to
`ComingSoonPage` until then. Populate the standard subfolders per
`docs/coding-standards.md` §4 when F6 starts.
