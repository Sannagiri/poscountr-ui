// Deprecated — tenant editing moved from this standalone route+tabs page
// into `TenantEditDrawer` (`../components/TenantEditDrawer`), a slide-over
// opened directly from a row click in `TenantsPage` instead of a full page
// navigation (see POSCountr-UI-Planning/progress/09_tenant_edit_drawer_date_picker.md).
// Nothing imports or routes to this file anymore; it's kept as an inert
// placeholder rather than deleted since the sandbox this was built in can't
// remove files from the mounted workspace folder.
export {};
