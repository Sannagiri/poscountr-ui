// Deprecated — the tenant edit dialog moved from this side-sliding `Drawer`
// into `TenantEditModal` (`../TenantEditModal`), a centered `Modal` —
// the same dialog type `TenantAdminsModal` uses, so every overlay on the
// Tenants screen now reads as the same kind of dialog instead of two
// different shapes (see POSCountr-UI-Planning/progress/12_cards_only_tenant_modal.md).
// Nothing imports this file anymore; it's kept as an inert placeholder
// rather than deleted since the sandbox this was built in can't remove
// files from the mounted workspace folder.
export {};
