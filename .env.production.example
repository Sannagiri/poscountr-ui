# Reference values for the production build. CI/CD supplies these as build-args
# to `docker/Dockerfile.production` — this file is documentation, not consumed directly.

VITE_API_BASE_URL=https://api.poscountr.com/api/v1
VITE_APP_STAGE=production
