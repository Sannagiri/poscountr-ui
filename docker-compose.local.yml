services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - '3200:3200'
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env.local
    command: npm run dev -- --host
